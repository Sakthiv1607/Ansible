---
- name: Run Ubuntu Terminal Commands
  hosts: all
  tasks:
    - name: Run a command
      shell: sudo -S -k <<< "Ubun2@L0gin" cat /etc/shadow
      become: yes
      register: hosts

    - name: Display command output
      debug:
        var: hosts.stdout
